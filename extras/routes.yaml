routes:
  /:
    template: home
    data: page.home
  /tags/:
    template: tags
  /rss/:
    template: rss
    content_type: text/xml

collections:
  /posts/:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
