routes:
  /:
    template: home
    data: page.home
  /tags/:
    template: tags

collections:
  /posts/:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
